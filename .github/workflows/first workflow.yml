name: My first workflow

on: push

jobs:
    first_job:
      runs-on: ubuntu-latest
      steps: 
      - name: checkout repo
        uses: actions/checkout@v4
        
      - name: Welcome message
        run: echo "My first github action job - Anuhya is a good girl and VENU is very bad bad fellow"